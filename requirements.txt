# Core (required)
flask==3.0.3
flask-cors==4.0.1
flask-limiter==3.8.0
python-dotenv==1.0.1
httpx==0.27.2
PyJWT==2.9.0
reportlab==4.2.5

# Production server (optional, used by the gunicorn command in the README)
gunicorn==23.0.0

# Optional integrations — only needed if you configure the matching
# provider in .env. The app runs fine without them (mock/in-memory
# fallbacks are used instead).
# supabase==2.7.4        # SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY
# pinecone==5.3.1         # PINECONE_API_KEY
# pypdf==4.3.1            # PDF document uploads to the knowledge base
# python-docx==1.1.2      # DOCX document uploads to the knowledge base
# redis==5.0.8            # REDIS_URL (production task queue)
# rq==2.10.0              # REDIS_URL (production task queue)
